# Path on host to this plugin repository (used by docker-compose override)
UNIFI2NETBOX_PLUGIN_PATH=/absolute/path/to/unifi2netbox

# Optional secret references for controller credentials (set in plugin UI as env:VAR)
UNIFI_API_KEY=
UNIFI_USERNAME=
UNIFI_PASSWORD=
UNIFI_MFA_SECRET=
